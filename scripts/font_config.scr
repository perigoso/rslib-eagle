# Font Configuration Script
# Author: Rafael Silva
# https://github.com/gimbas/rslib

display torig borig;
group all;
smash (C>0 0);
display none tname bname tval bval;
group all;
change Align center (C>0 0);
change font vector (C>0 0);
change size 0.8mm (C>0 0);
change ratio 12 (C>0 0);
display last;
